按照PRD.nd的需求要求，生成完整的RPi示波器应用程序的SPEC.md文件，其中包括系统SPEC和模块SPEC两部分，此文档用于后续开发应用程序，并确保合理的模块化，每个模块都要有自己的单元测试；要在最终程序有bug或有需求更改时，准确的定位到某个模块，修改时也可以只修改该模块的代码，而不影响其他模块的功能。

根据PRD.md的需求要求，SPEC.md中的系统结构和模块spec，开始开发完整应用程序。如果遇到PRD.md和SPEC.md中有冲突的，以PRD.md为准。如果PRD.md没有明确说明又非常重要的，选你认为最好的选择并记录下问题到md文件，最后汇总给我reivew。你可以边开发边测试，尽量不要中断问我授权，你可以在当前目录下自由操作文件和目录，如果需要额外动作来避免中断下来运行sudo xxx的指令，告诉我我们来解决。

运行PYTHONPATH=src .venv/bin/python -m rpiosc.app后可以正常启动图形界面，但是左上页面没有显示实时收到的信号图像。

按照PRD.md的## 示波器设置按钮 章节，在左下控制界面上实现这些按钮的控制

重新说明三种触发模式的含义：
1. 单次触发：第一次触发之后，左上窗口保持显示这个触发的画面，并且不再进行新的触发
2. 手动触发：每次触发后，左上窗口保持显示这个触发的画面，直到下一次触发发生
3. 自动触发：每次触发后，左上窗口保持显示这个触发的画面2秒钟或者下一次触发发生，之后恢复显示实时信号画面
按照上面说明更新代码和PRD.md

键盘快捷键显示在按钮上面，放在按钮名下方加括号显示。
四个通道开关按钮的颜色：当前通道关闭时，为灰色按钮（与现在的设计相同），当前通道打开时，按钮背景色为通道对应的颜色设置。
按照上面说明更新代码和PRD.md

发现有问题：仅打开通道1时，并非仅显示黄色曲线，左上屏幕上同时有其他颜色的线。运行时截图见Pasted image.png。排查问题。

在左上窗口x和y轴显示的数字加上单位。只需要加在最大一个显示的数字后面，以提示用户当前的显示范围。按照上面说明更新代码和PRD.md

当前输入信号是10hz的方波，但是实时窗口显示的是三角波。见屏幕截图Pasted image.png。分析并修复问题，使正确显示实时波形。


由于GPIO的输入为数字信号，要么0,要么1,所以固定Y轴的显示范围为0-1,并且取消Y scale和Y position调整的四个按钮。当调整X scale改变div时长跨度时，X轴的显示范围要相应调整，保持显示5个Div。同时仅刷新显示当前5个div内的曲线内容即可，这样当div代表的时间跨度变小时，画面显示需要刷新的数据量也会减小。同时要设置采样的优先级最高，不要让显示刷新干扰GPIO的采样，比如当前加载1khz的方波，实时波形显示截图见Pasted image.png，这里面有明显丢点。修复问题，使正确显示实时波形。按照上面说明更新代码和PRD.md，修复不准确的需求理解。

当前显示刷新频率是多少？为了尽可能减少采样丢点，可以降低显示刷新率到10fps

在主界面按下按钮上显示的对应快捷键无相应。排查并解决问题。
X轴始终显示从0开始，并向正向显示，比如初始时显示0-1s，调整scale之后显示0-xxxms的显示范围

去掉Trig Level Up和Level Down按键，增加Trig Position Left 和Trig Position Right按键，用于调整触发的显示位置。将触发位置处用垂直虚线标出。虚线颜色同触发的通道中边沿触发的那个通道颜色。


将通道234的Y值显示加上一点点不同的offset，让各通道的电平显示在略有不同的高度以方便观察。

右上窗口没有显示最后一次的触发的主屏幕显示（波形显示窗口），分析并修复。

在左下控制区域显示当前的触发条件。

如图Pasted image.png，当前通道1接高电平，通道2接1khz方波，但是不停的在触发，这不符合当前的触发条件。分析并修复。

当前的触发条件如何修改？在左下窗口安排触发条件的设置界面。另外当前通道1和通道2都加载了1khz方波，但是没有产生任何触发。分析并修复。

当通道1和通道2都加载1khz方波时，确实不停的产生了当前条件的触发。但是当通道1保持高电平，通道2加载1khz方波时仍然是不停的在触发。分析触发条件的逻辑判断并修复问题，仅当采样到符合触发逻辑条件的时候触发。

每次启动程序时，如果trigger_log.csv记录超过1000条，则仅保留最近发生的1000条记录。更新PRD.md

问题没有解决，仅有通道2加载1khz方波时，仍在不停触发。已经改了好几轮了，你仔细分析一下问题，修改后自行运行程序，如果程序运行正确，不会在trigger_log.csv不停的增加新的触发记录。

你对数字示波器的触发概念理解有误。按照下面的解释来实现代码。Trig Auto, Trig Normal, Trig Single并不影响触发条件，仍然是当信号满足给定的触发条件时才产生触发。Auto Normal指的是触发后的显示方式不同。Auto模式下，没有触发时主窗口显示实时波形，触发后主窗口停留在触发时画面一定时间（2s）后继续显示实时波形；Normal模式下，设置后首次触发前主窗口显示实时波形，触发后则停留在触发时画面直到下一次触发；Single模式下，设置后首次触发前主窗口显示实时波形，触发后则停留在触发时画面且不再进行新的触发，直到用户下一次操作（再次按下Single按键或者设置其他操作模式）；在触发列表中，所有的记录都是因为信号满足触发的逻辑条件而不是因为其他原因。按照这个逻辑修改不同触发模式下的功能，并更新PRD.md中对触发模式的理解。

你说的对，在我将通道2在外部由浮空改为接地后则没有异常触发了。现在另一个问题是，在auto模式下，如果没有触发，则主窗口始终没有显示实时波形，这不是我们要的。在Auto模式下，没有触发时主窗口应当以10fps显示实时波形。

当前auto模式还是需要调整：当前程序刚启动没有触发时行为正确，是显示实时波形。但是在触发后主界面就停在触发界面了，而要求是停在触发界面2秒钟后继续显示实时波形，如果再有新的触发，则再次重新显示新的触发波形2秒钟；如果在某次触发后显示触发波形的2秒内又有新的触发，则显示新触发的波形并重新计时显示触发波形2秒钟。

启动时Y轴范围默认显示0-1.5以便显示出所有通道Y轴全貌。每次触发后在生成触发记录的同时，将当前主屏幕画面显示在右上窗口同时将主窗口画面保存为png文件，命名格式为Trig_加上日期时间.png，同时每次开机时，仅保留最近生成的1000个png 触发截图。更新PRD.md

auto模式下，触发后2秒恢复显示实时波形时显示的波形有问题，画面好像只刷新的一部分，见截图Pasted image.png

在右下触发纪录窗口增加一个“清除触发记录”的按钮，用于清除csv文件的内容并且删除所有的触发png文件。更新PRD.md

run app under ~/osc_rpi with command "PYTHONPATH=src .venv/bin/python -m rpiosc.app", and serials trigging already happen but no png file found under ~/osc_rpi/screenshots

yes I have seen screenshots png files but all of them are black with nothing. fix it. I want each png save the main screen(left up) of each trigging.

当前显示分辨率降低了，导致左上窗口显示不全。将左下窗口所有按钮的快捷键说明放到按钮文字同一行，减小按钮高度。将左下窗口中fs,mode,X,TrigPos,的显示放到同一行节省垂直空间。Holdoff,CPU,MEM的显示放到同一行节省垂直空间。

默认触发条件改为：
第1通道下降沿后的1ms之内通道2出现了边沿跳变（上升或下降），则触发

默认触发条件改为：
第1通道出现了边沿跳变（上升或下降），则触发


这种情况下我希望处发现画在CH1上。现在程序运行仍然有问题，和之前的现象很相像：现在触发后CH2的电平状态对的，但是出发前CH2的状态显示的部队。CH1始终保持了低电平，在触发前后的图像都没有任何变化，而实际上是有明确方波的应该。
当我使用CH1 Both作为触发条件时，观察到在出发前的图像看上去正确，但是触发后的图像没有任何变化。此时CH2也没有观察到任何变化（但无法判断是否正确）仔细分析触发产生时用于重绘图形的数据点是如何得来的，我要触发时刻前后的在显示窗口中的时间段内的信号都可以真实的显示出来并抓取到。如果需要显示响应可以慢一点没关系。另外触发之后的一定时间内也可以不产生触发（比如当前触发显示窗口内的其他触发可以不产生和记录。

触发时的画面看，触发时刻前各通道的波形都没有正确的捕捉到，触发之后的波形是正确的。触发之前需要显示出来的时间内的数据都要能正确的显示出来，并且第一个边沿事件之前的电平状态也要正确的显示出来。

启动时默认x div改为2ms，标题栏信息中增加递增版本号显示。

==TODO

当前触发时左上主窗口显示正确，但是保存的png图形以及右上角显示的触发截图不正确，没有显示出需要的内容。

特有需求：每次触发时，在对主窗口显示时间范围内的信号，分别计数通道1,2的下降沿个数，并记录在触发记录列表中。

当前显示仅显示了第二通道（绿色），第一通道未显示。
并没有修复，并且连第二通道的实时波形也没有显示出来。

当前窗口是根据PRD.md编写的数字示波器app，用于抓取特定条件的信号并保存附近的实时波形。当前实时显示画面（左上窗口）显示不正确，在auto模式下，各个通道的实时信号都没有显示出来。分析问题并修复。

告诉我当前GPIO上的采样方法，是仅采集边沿信号还是以一定采样频率持续采样的？

启动程序后在没有加载触发信号的情况下，没有实时的显示信号的实时波形。各个通道的信号都没有实时波形显示，分析并解决问题。

综合考虑下面几个存在的问题，分析代码中的问题并修复：
1. 当前设置的是通道1双边沿（上升沿或者下降沿）触发，但是运行时仅通道1上升沿时产生了触发，下降沿没有产生触发。
2. 当前在通道1边沿触发后的1ms内有存在通道2的边沿事件，但是触发时抓到的实时波形并没有通道2的信号变化
3. 触发事件产生后（右下窗口产生事件记录）短暂在主窗口固定显示的实时波形中通道1和通道2都未见任何变化，参见截图Pasted image.png
以上问题说明采样和波形显示存在严重的错误，导致基本功能不可用，分析问题并修复以上几个问题。

仍然有问题，当前每个通道1的上升沿都会触发很多次脉冲。分析采样和触发机制，同时为了确保显示正确的通道波形，写一个自动测试的功能，你自己模拟注入满足条件的触发，然后检查实时画面显示是否正确。给你的外部测试环境如下：通道1加载1khz方波，通道2常为0. 设计测试验证1.实时波形显示正确，实时波形能够正确显示通道上的信号，2.触发条件满足时可以正确触发并且可以在主窗口正确显示触发时的信号波形画面，3.触发条件满足时可以正确记录事件和主窗口实时波形画面的png截图。使用上面的测试验证功能并进行迭代。

对触发操作的代码处理上，要设置一个触发开关标志，用处是比如在Single模式下第一次触发后，触发开关就要关闭，再次按下Single按键或者设置其他触发模式时再打开此开关，由此来控制触发的使能。


//ADC, Rpi5 donot have ADC
要呈现的是一个示波器的功能，不是只显示变化的点，而是要显示真实的波形形状。Y轴也不是只显示0或1,而是要以ADC来采样后计算的实际电压值来显示。左上窗口曲线的描点密度可以按照当前显示分辨率变化，但是在点抽取时要能体现最值特征。修复问题，使正确显示实时波形。按照上面说明更新代码和PRD.md，修复不准确的需求理解。




禁用鼠标在左上窗口的zoom in zoom out操作，左上窗口的坐标轴显示调整都通过左下面板的按钮或键盘快捷键完成。


